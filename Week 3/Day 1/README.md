# Day 1 — Supervised Learning Concepts & the Scikit-learn API

## Learning Objectives

- Explain supervised learning and distinguish regression from classification
- Separate a dataset into features (X) and target (y)
- Perform a train/test split and explain why evaluating on unseen data is essential

## What Was Done

- Loaded a provided dataset and separated it into features `X` and target `y`
- Performed an 80/20 train/test split with a fixed `random_state` using `train_test_split`
- Confirmed and printed the shapes of `X_train`, `X_test`, `y_train`, `y_test` for consistency
- Explained in a Markdown cell why the model must never see the test set during training

## Dataset

House price dataset

## Tools Used

Scikit-learn • Pandas • Jupyter Notebook